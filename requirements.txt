# Direct dependencies only (transitive deps like numpy, requests are pulled in
# automatically). Requires Python 3.12+ (nextcord 3.2.0 floor).

# Discord
nextcord==3.2.0
nextcord-ext-menus==1.5.7

# HTTP / async I/O
aiohttp==3.14.1
aiofiles==25.1.0

# External service clients
qbittorrent-api==2026.6.0

# Data & visualization
pandas==3.0.3
matplotlib==3.11.0
seaborn==0.13.2

# Timezones: zone lookups use stdlib zoneinfo, which needs an IANA database —
# tzdata supplies it on platforms without a system copy (Windows, slim containers);
# tzlocal detects the local zone.
tzdata==2026.2
tzlocal==5.4
